14. SPA (Single Page Application)

React apps are SPAs → load only one HTML (index.html) and update dynamically without full page reload.

Interview Qs

Q: What is SPA?
A: A web app that loads a single HTML page and updates content dynamically.

Q: Why is React considered SPA framework?
A: Because React updates UI via Virtual DOM without reloading the whole page.
